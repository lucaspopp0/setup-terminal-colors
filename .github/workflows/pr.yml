name: pr

on:
  pull_request:

permissions:
  contents: read
  pull-requests: read

defaults:
  run:
    shell: bash

jobs:

  validate:
    runs-on: ubuntu-latest
    steps:
      -
        uses: actions/checkout@v6
      -
        uses: ./.
      -
        name: Print some colored content
        run: |
          echo -e "${BOLD}Bold text${RESET}"
          echo -e "${GREEN}GREEN text${RESET}"
          echo -e "${RED}RED text${RESET}"
